# python version used: Python 3.7.6
# git clone repository
#create vitrual env
cd to directory with repository
python3 -m venv acled_gn
# activate environment
source acled_gn/bin/activate
# install requirements
pip3 install requirements.txt

# execute
python3 acled_gn.py
# a shapefile with the ACLED events of the last 30 days will be generated in data/ folder
# the log file in logs/ folder will be updated

# upload the layer to geonode (and delete old layer)
python3 migration.py -c /Users/dimitriskarakostis/workspaces/work/syr_acled_geonode/acled_download/config.json
